# Say It — 寫下此刻 ✍️

一個極簡、優雅且專注的隨手寫作、程式碼暫存與 AI 輔助空間。

---

## 💡 專案初衷
本專案的誕生，源於作者平常有一些**臨時的代碼片段**、**靈感想法**或**待辦備忘**需要隨手記錄，但又**不想在電腦中建立一堆零散的實體檔案（如 `temp.txt`、`test.py`）**。

因此，開發了這個網頁版隨手寫作空間：
- **無需建立檔案**：所有內容自動儲存在瀏覽器的 `LocalStorage` 中。
- **無縫自動存檔**：停止輸入 `450 毫秒` 後自動在背景存檔，不用擔心忘記存檔或瀏覽器崩潰。
- **支援程式碼模式**：提供輕量化語法高亮，適合臨時貼上代碼進行閱讀或編輯。
- **內建 AI 輔助工具**：隨時針對選取文字或代碼進行翻譯、修飾或解說，不用頻繁切換到其他 AI 視窗。
- **支援 PWA 離線使用**：可安裝至桌面或手機，隨開隨用，提供乾淨、無干擾的書寫環境。

---

## 🛠️ 功能特點
1. **多分頁管理**：可自訂分頁名稱、小標題，並指定專屬的配色與模式。
2. **語法高亮（Syntax Highlighting）**：支援 `一般文字 (.txt)`、`Python (.py)`、`SQL (.sql)`、`TypeScript (.ts)`、`C# (.cs)` 等格式的高亮顯示，並提供 Midnight、Forest、Paper 三種程式碼配色。
3. **AI 工具助理（對接 Gemini）**：
   - 🔍 **智慧翻譯**：自動偵測來源語言，一鍵翻譯成繁體中文、英文或越南文。
   - ✨ **文字優化**：提供「自然流暢」、「正式專業」、「精簡清楚」、「修正文法與錯字」等風格優化。
   - 💻 **程式碼解說與註解**：自動分析選取的程式碼，並在不影響程式行為的前提下，加入清晰的繁體中文註解。
4. **API Key 安全設計**：可選擇在瀏覽器中記住 API Key，或是不勾選記住（僅留存於 Session，關閉分頁即刪除）。
5. **AI 結果比對與防呆**：
   - 採用並排（Side-by-Side）對照視窗，清楚比對原文與 AI 處理結果。
   - 支援「一鍵取代選取文字」或「複製結果」。
   - 防呆機制：若套用結果時使用者已切換分頁或更動原文，系統會發出警示以防覆蓋錯誤。
6. **字數與詞數統計**：即時統計字數（不含空白）與中英文詞數。
7. **快捷功能**：
   - **`F5` 鍵**：快速在游標處插入當前時間戳記（例如 `2026-07-15 13:50`）。
   - **`Tab` 鍵**：在程式碼模式下，按下 Tab 會插入兩個空白縮排。
   - **`Ctrl + S`** 或 **`Cmd + S`**：強制手動存檔。
8. **深淺色主題切換**：自動適應系統主題，亦可手動切換深淺色模式，並動態變更瀏覽器主題顏色（Theme Color）。
9. **一鍵複製與匯出**：可快速複製全文，或將內容直接下載為對應副檔名的實體檔案。

---

## 📂 檔案目錄架構
```
SayIt/
├── index.html           # 應用程式的主頁面結構、AI 工具對話框與 Preview 視窗
├── app.js               # 核心邏輯（LocalStorage 儲存、自訂分頁、自建語法高亮引擎、事件監聽）
├── ai-service.js        # AI 核心模組（Gemini API 串接、Prompt 模板構建與錯誤處理）
├── ai-ui.js             # AI 介面控制器（選取範圍監聽、彈窗展示、API Key 儲存邏輯、防呆防覆蓋套用）
├── styles.css           # 響應式與主題化 CSS（支援 AI 對照預覽視窗、毛玻璃特效、深淺色切換）
├── sw.js                # Service Worker 腳本（用於靜態資源快取以支援 PWA 離線使用，已納入 AI 相關檔案）
├── manifest.webmanifest # PWA 設定檔（定義應用程式圖示、啟動路徑及顯示模式）
├── icon.svg             # 應用程式圖示（向量格式，供 PWA 與瀏覽器使用）
├── .gitignore           # Git 忽略設定檔（排除編輯器與開發代理工具的暫存檔）
└── README.md            # 專案說明文件（本檔案）
```

---

## 🏗️ 程式碼架構解析
整個專案採用無框架的 **Vanilla JS (原生 JavaScript)** + **原生 CSS 變數** 進行開發，以極致的載入速度和極低的資源佔用為目標。

### 1. 狀態與資料管理 (`app.js`)
- **資料持久化**：使用 `localStorage` 保存分頁配置（`sayit-custom-tabs-v2`）與文字內容（`sayit-tab-content-<tabId>`）。
- **防抖動儲存 (Debounced Auto-save)**：監聽編輯器的 `input` 事件，利用 `setTimeout` 實作 450ms 延遲存檔，避免頻繁寫入 LocalStorage 影響網頁效能。

### 2. 語法高亮引擎 (`app.js` & `styles.css`)
為了不載入龐大的第三方代碼高亮庫（如 Prism 或 Highlight.js），專案實作了**輕量級的程式碼語法高亮 overlay**：
- **雙層堆疊 (`.editor-surface`)**：底層為一個 `pre` 元素（負責顯示高亮後的 HTML），頂層為一個 `textarea`（字體顏色設為 `transparent`、插入符 caret 設為可見）。
- **同步捲動**：透過監聽 `textarea` 的 `scroll` 事件，同步更新 `pre` 的 `scrollTop` 與 `scrollLeft`。

### 3. AI 模組與 API 整合 (`ai-service.js` & `ai-ui.js`)
- **Serverless 直接呼叫**：客戶端直接對接 Google Gemini API，無須搭建中間伺服器，速度更快且保障隱私（Key 儲存在使用者本地）。
- **智慧 Prompt 模板構建 (`buildPrompt`)**：
  - **翻譯**：自動偵測語言，翻譯成目標語言，並保留代碼、人名、專有名詞等不應翻譯的內容。
  - **修飾**：根據選定風格（自然、正式、精簡等）調整文字，並支援自動翻譯為使用者選擇的輸出語言。
  - **程式碼註解**：精確控制 Gemini **只增加註解**，不改變任何代碼邏輯或格式，並使用該程式語言的語法（如 `#` 或 `//`）。
- **狀態防呆校驗**：套用 AI 修改時，會先檢查使用者當前的 activeTabId 是否與選取時相同，並比對編輯器中該選取區間的原文是否已被使用者改動。若有任何變化，會阻止覆蓋並彈出警告，確保代碼安全性。

---

## 📈 修改記錄與更新日誌
### **2026-07-15**
- ✨ **新增 AI 工具整合**：
  - 導入 Google Gemini 2.5 Flash 服務，提供即時的翻譯、文字優化及程式碼解說註解功能。
  - 實作 API 設定彈窗，支援自訂模型名稱、API Key 的記憶/單次會話存取。
  - 實作 Side-by-Side 「確認 AI 結果」比對對話框。
  - 實作套用變更時的「分頁防呆」與「原文更動保護」，預防代碼被錯誤覆蓋。
- ⚙️ **核心與樣式優化**：
  - 修正初始化載入時主題色 `metaThemeColor` 沒同步跟隨深淺色載入的 Bug。
  - 重構並改進 `save-state` 狀態欄的 DOM 選取方式，改用 Class 方式更新文字。
  - 清理 [styles.css](file:///D:/AI/SayIt/styles.css) 中殘留的 Legacy `phrasebook` 舊版多餘樣式，精簡檔案大小。
  - 將新寫的 `ai-service.js` 與 `ai-ui.js` 加進 Service Worker ([sw.js](file:///D:/AI/SayIt/sw.js)) 離線快取資產清單中，保證 PWA 安裝版離線使用正常。
- 📝 **新增專案 README.md**：
  - 詳細記錄專案起源、功能、檔案目錄以及 Vanilla JS / 語法高亮 Overlay 的程式碼底層架構。

---

## 🚀 如何在本地運行
本專案為純前端靜態網頁，無需搭建後端伺服器即可運行：
1. 下載或 Clone 本專案。
2. 連按兩下 `index.html` 即可在瀏覽器中開啟。
3. 若要啟用 PWA 完整功能（如離線快取與安裝），建議使用簡易網頁伺服器（例如 VS Code 的 Live Server 插件，或是執行 `npx serve`）透過 `http://localhost` 開啟。
